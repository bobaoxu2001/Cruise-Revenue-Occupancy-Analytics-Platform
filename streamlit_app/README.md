# Streamlit AI Analyst Demo

This is a governed question-answering prototype. It intentionally uses a controlled menu rather than promising unrestricted natural language analytics.

Run it after building dbt and exporting extracts:

```bash
streamlit run streamlit_app/app.py
```

Each answer displays the metric definition, query logic, table output, and a chart where useful.
