name: cruise_analytics
version: "1.0.0"
config-version: 2
profile: cruise_analytics

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  cruise_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
    semantic:
      +enabled: false

seeds:
  cruise_analytics:
    +quote_columns: false
